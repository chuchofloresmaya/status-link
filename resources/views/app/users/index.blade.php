<x-app.layout title="Usuarios"><x-ui.page-header title="Usuarios de la notaría" description="Administra únicamente las cuentas de tu organización." :action="route('app.users.create')"/><x-ui.table><thead class="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr>@foreach(['Nombre','Email','Rol','Estado','Acciones'] as $h)<th class="px-4 py-3">{{ $h }}</th>@endforeach</tr></thead><tbody class="divide-y divide-slate-100">@forelse($users as $u)<tr><td class="px-4 py-3 font-medium">{{ $u->name }}</td><td class="px-4 py-3">{{ $u->email }}</td><td class="px-4 py-3">{{ str_replace('_',' ',$u->getRoleNames()->first()??'—') }}</td><td class="px-4 py-3"><x-ui.badge :status="$u->is_active"/></td><td class="px-4 py-3"><div class="flex gap-2"><x-ui.button variant="secondary" :href="route('app.users.edit',$u)">Editar</x-ui.button>@if(!auth()->user()->is($u))<form method="POST" action="{{ route('app.users.toggle-active',$u) }}">@csrf @method('PATCH')<x-ui.button variant="secondary" type="submit">Cambiar estado</x-ui.button></form>@endif</div></td></tr>@empty<tr><td colspan="5"><x-ui.empty-state>No hay usuarios en tu notaría.</x-ui.empty-state></td></tr>@endforelse</tbody></x-ui.table>{{ $users->links() }}</x-app.layout>
