<x-app.layout title="Dashboard de notaría"><x-ui.page-header :title="$notary->name" description="Resumen de tu organización y configuración."/><div class="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">@foreach(['Plan actual'=>$notary->activeSubscription?->plan?->name ?? 'Sin plan','Usuarios activos'=>$notary->active_users_count,'Perfiles activos'=>$notary->active_notarial_profiles_count,'Cuentas activas'=>$notary->active_bank_accounts_count,'Perfil predeterminado'=>$notary->defaultNotarialProfile?->name ?? 'Sin definir','Cuenta general predeterminada'=>$notary->defaultBankAccount?->bank_name ?? 'Sin definir'] as $label=>$value)<x-ui.card><p class="text-sm font-medium text-slate-500">{{ $label }}</p><p class="mt-2 text-xl font-bold">{{ $value }}</p></x-ui.card>@endforeach</div></x-app.layout>
