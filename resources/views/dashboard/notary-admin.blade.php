<x-app.layout title="Dashboard de notaría"><x-ui.page-header :title="$notary->name" description="Resumen de tu organización y suscripción."/><div class="grid gap-4 sm:grid-cols-2 xl:grid-cols-5">@foreach(['Notaría'=>$notary->name,'Plan actual'=>$notary->activeSubscription?->plan?->name ?? 'Sin plan','Suscripción'=>$notary->activeSubscription?->status ?? 'Sin suscripción','Usuarios activos'=>$notary->active_users_count,'Límite de usuarios'=>$notary->activeSubscription?->plan?->limits['users'] ?? 'Sin límite'] as $label=>$value)<x-ui.card><p class="text-sm font-medium text-slate-500">{{ $label }}</p><p class="mt-2 text-xl font-bold">{{ $value }}</p></x-ui.card>@endforeach</div></x-app.layout>
