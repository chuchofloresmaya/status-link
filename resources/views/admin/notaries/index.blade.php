<x-app.layout title="Notarías"><x-ui.page-header title="Notarías" description="Organizaciones registradas en la plataforma." :action="route('admin.notaries.create')"/><x-ui.table><thead class="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr>@foreach(['Nombre','RFC','Contacto','Estado','Usuarios','Plan','Acciones'] as $h)<th class="px-4 py-3">{{ $h }}</th>@endforeach</tr></thead><tbody class="divide-y divide-slate-100">@forelse($notaries as $notary)<tr><td class="px-4 py-3 font-medium">{{ $notary->name }}<br><span class="text-xs text-slate-500">{{ $notary->slug }}</span></td><td class="px-4 py-3">{{ $notary->rfc ?: '—' }}</td><td class="px-4 py-3">{{ $notary->email ?: '—' }}<br>{{ $notary->phone }}</td><td class="px-4 py-3"><x-ui.badge :status="$notary->is_active"/></td><td class="px-4 py-3">{{ $notary->users_count }}</td><td class="px-4 py-3">{{ $notary->activeSubscription?->plan?->name ?? '—' }}</td><td class="px-4 py-3"><div class="flex flex-wrap gap-2"><x-ui.button variant="secondary" :href="route('admin.notaries.edit',$notary)">Editar</x-ui.button><x-ui.button variant="secondary" :href="route('admin.notaries.profiles.index',$notary)">Perfiles</x-ui.button><x-ui.button variant="secondary" :href="route('admin.notaries.bank-accounts.index',$notary)">Cuentas</x-ui.button><form method="POST" action="{{ route('admin.notaries.toggle-active',$notary) }}">@csrf @method('PATCH')<x-ui.button variant="secondary" type="submit">{{ $notary->is_active?'Desactivar':'Activar' }}</x-ui.button></form></div></td></tr>@empty<tr><td colspan="7"><x-ui.empty-state>No hay notarías registradas.</x-ui.empty-state></td></tr>@endforelse</tbody></x-ui.table><div class="mt-5">{{ $notaries->links() }}</div></x-app.layout>
