<x-app.layout title="Pagos"><x-ui.page-header title="Pagos manuales" description="Movimientos registrados sin pasarela externa." :action="route('admin.payments.create')"/><x-ui.table><thead class="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr>@foreach(['Notaría','Suscripción','Monto','Método','Referencia','Estado','Fecha','Registró'] as $h)<th class="px-4 py-3">{{ $h }}</th>@endforeach</tr></thead><tbody class="divide-y divide-slate-100">@forelse($payments as $p)<tr><td class="px-4 py-3 font-medium">{{ $p->notary->name }}</td><td class="px-4 py-3">{{ $p->subscription?->plan?->name??'—' }}</td><td class="px-4 py-3">{{ $p->currency }} ${{ number_format((float)$p->amount,2) }}</td><td class="px-4 py-3">{{ $p->payment_method??'—' }}</td><td class="px-4 py-3">{{ $p->reference??'—' }}</td><td class="px-4 py-3"><x-ui.badge :status="$p->status"/></td><td class="px-4 py-3">{{ $p->paid_at?->format('d/m/Y')??'—' }}</td><td class="px-4 py-3">{{ $p->creator?->name??'—' }}</td></tr>@empty<tr><td colspan="8"><x-ui.empty-state>No hay pagos registrados.</x-ui.empty-state></td></tr>@endforelse</tbody></x-ui.table>{{ $payments->links() }}</x-app.layout>
