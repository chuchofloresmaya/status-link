<x-app.layout title="Suscripciones"><x-ui.page-header title="Suscripciones" description="Historial de membresías por notaría." :action="route('admin.subscriptions.create')"/><x-ui.table><thead class="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr>@foreach(['Notaría','Plan','Estado','Inicio','Fin','Trial','Acciones'] as $h)<th class="px-4 py-3">{{ $h }}</th>@endforeach</tr></thead><tbody class="divide-y divide-slate-100">@forelse($subscriptions as $s)<tr><td class="px-4 py-3 font-medium">{{ $s->notary->name }}</td><td class="px-4 py-3">{{ $s->plan->name }}</td><td class="px-4 py-3"><x-ui.badge :status="$s->status"/></td><td class="px-4 py-3">{{ $s->starts_at->format('d/m/Y') }}</td><td class="px-4 py-3">{{ $s->ends_at?->format('d/m/Y')??'—' }}</td><td class="px-4 py-3">{{ $s->trial_ends_at?->format('d/m/Y')??'—' }}</td><td class="px-4 py-3"><div class="flex gap-2"><x-ui.button variant="secondary" :href="route('admin.subscriptions.show',$s)">Ver</x-ui.button>@if($s->status==='active')<form method="POST" action="{{ route('admin.subscriptions.cancel',$s) }}">@csrf @method('PATCH')<x-ui.button variant="danger" type="submit">Cancelar</x-ui.button></form>@endif</div></td></tr>@empty<tr><td colspan="7"><x-ui.empty-state>No hay suscripciones.</x-ui.empty-state></td></tr>@endforelse</tbody></x-ui.table>{{ $subscriptions->links() }}</x-app.layout>
